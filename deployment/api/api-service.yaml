apiVersion: v1
kind: Service
metadata:
  labels:
    app: api
  name: api
  namespace: api
spec:
  ports:
    - name: "http"
      port: 8000
      targetPort: 8000
  selector:
    app: api
status:
  loadBalancer: {}
